!!!
%html
  %head
    %title= @title || "LilyBlog"
    = stylesheet [:reset, :tags]
  %body
    %h1
      %a{ :href => '/' }= @title || "LilyBlog"
    = yield
    #tag_cloud
      %h4 Tags
      - Post.tags.each do |tag, amount|
        %span.tag{ :class => Post.tag_class(tag) }
          = tag

